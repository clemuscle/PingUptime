name: CI/CD PingUpTime

on:
  push:
    branches: [ main ]

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      - name: Install deps & run tests
        run: |
          pip install -r requirements.txt

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}
          platforms: linux/amd64
          outputs: type=registry

  deploy:
    needs: test_and_build
    runs-on: ubuntu-latest

    steps:
      - name: Load SSH key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.OCI_PRIVATE_KEY }}

      - name: Deploy to OCI VM
        run: |
          echo "Pulling new image..."
          ssh ubuntu@${{ secrets.INSTANCE_PUBLIC_IP }} \
            "docker pull ${{ secrets.REGISTRY }}:${{ needs.test_and_build.outputs.image_tag }} && \
             docker rm -f pinguptime || true && \
             docker run -d \
               --name pinguptime \
               --restart unless-stopped \
               -p 5000:5000 \
               ${{ secrets.REGISTRY }}:${{ needs.test_and_build.outputs.image_tag }}"